# shared

Shared types and utilities used by every other package.

- TypeScript types for `Employee`, `Action`, `Approval`, `AuditLog`, etc.
- Supabase client (singleton)
- Brand memory loader — reads `/memory/*.md` and exposes them to agents
